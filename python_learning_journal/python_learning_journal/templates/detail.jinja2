{% extends "layout.jinja2" %}
{% block body %}
    <article>
        <p>id: {{ entry.id }}</p>
        <h1>{{ entry.title }}</h1><p>{{ entry.creation_date }}</p>
        <p>{{ entry.body }}</p>
    </article>
{% endblock %}
